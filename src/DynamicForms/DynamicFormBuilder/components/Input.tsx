// import { InputConfig } from "../../../types/alltypes.type";

// const Input = (props: InputConfig) => {
//   return (
//     <input
//       name={name}
//       value={form[input.name] || ""}
//       onChange={(e) => handleInput(props, e)}
//       placeholder={placeholder}
//       className={`${classPrefix}-${defaultInputClass}`}
//     />
//   );
// };

// export default Input;
